<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Make Server Action for Merge Invoice -->
    <record id="model_merge_invoice" model="ir.actions.server">
        <field name="name">Merge Invoice</field>
        <field name="model_id" ref="practice_module.model_account_move"/>
        <field name="binding_model_id" ref="practice_module.model_account_move"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_combine_merge_invoice()</field>
    </record>
</odoo>